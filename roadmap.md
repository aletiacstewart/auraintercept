# Roadmap

- [x] Repair password-reset email links and add the new-password screen.
- [ ] Talk to Aura broken: elevenlabs-aura-token returns 400 "API key ID used as API key" — blocked on real ElevenLabs sk_ key (user must rotate/copy actual key, then retest).
