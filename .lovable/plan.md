Update the homepage cinematic-hero CTA button in `src/pages/Index.tsx`.

1. Change button text from `Deploy Your AI Workforce` to `Start Here`.
2. Change `onClick` navigation from `/auth?mode=company` to `/for-business` (the Live Demo / industry picker page).

No other pages or components affected.