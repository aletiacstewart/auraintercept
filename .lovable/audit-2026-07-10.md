# Full Platform Audit — 2026-07-10

Coverage: 24-agent model + 10 operatives, 60+ edge functions, 33 live cron jobs, DB triggers, industry pack coverage (28 packs), onboarding, customer-facing surfaces, field ops, marketing/content/analytics, integrations, billing, hygiene.

## Severity summary

| Sev | Count |
|-----|-------|
| P0  | 0 |
| P1  | 0 |
| P2  | 4 |
| P3  | 12 |
| **Total** | **16** |

## Resolved in this pass

- ✅ P3 · `PricingComparisonTable` + `Subscription` copy updated: "Central control center" replaced with "Your hub" for AI Operatives Hub description.
- ✅ P3 · Shared trial constants module added at `supabase/functions/_shared/trial.ts` (TRIAL_DAYS, TRIAL_ONBOARDING_DAYS, TRIAL_LIVE_USE_DAYS, TRIAL_LABEL) as single source of truth for future edge-function refactors.
- ✅ P2 · `create-checkout` cold-start price assertion added: EXPECTED_MONTHLY_CENTS mirrors `launchPricing.ts` sale amounts and logs WARN in edge logs on drift.
- ℹ️ P2 · `send-email-guarded` and `campaign-track` "orphan" findings downgraded to false positives — both invoked by `send-campaign` (invoke() and tracking pixel URLs).
- ℹ️ P2 · Stripe Tee*/Tdvk* naming memory drift downgraded to false positive — no such naming exists anywhere in `.lovable/memory`.
- ✅ P2 · Anon JWT literals removed from trigger function bodies — moved to `_edge_anon_key` config table; `trigger_employee_welcome_on_profile` and `trigger_kb_refresh` now read the key at runtime.
- ✅ P2 · `industryKpiLabels.ts` OVERRIDES + SIMPLE_KPI_BY_INDUSTRY now cover all 28 industry packs.
- ℹ️ P2 · `useMultiAgentChat` naming drift downgraded to false positive — canonical edge fn is `ai-agent-chat`; hook name reflects a multi-agent UI, not a separate function.
- ✅ P2 · CRM `Sync Now` button relabeled to `Force sync` with helper text noting hourly `aura-crm-sync-leads` auto-sync.
- ✅ P2 · `industryVoiceGreetings.ts` extended with `dental`, `chiropractic`, `optometry`, `medical_office` (28/28 packs); the "stray templates key" claim was mistaken — none present.
- ✅ P0 · `orchestrator-proposals` producer implemented (idle leads/appointments/invoices → `agent_proposed_actions`).
- ✅ P1 · Duplicate cron jobs removed (7 pairs → single `aura-*` versions).
- ✅ P1 · Duplicate `trg_inventory_low_stock` trigger dropped.
- ✅ P1 · `integration-health-check` extended to probe Resend, ElevenLabs, Stripe, Tavily (was 2/8, now 6/8; A2P + Upload-Post still pending).
- ✅ P1 · Orphan edge functions `dispatch-integrations` and `verify-insurance` deleted (folders + `config.toml` entries + doc references).
- ✅ P3 · Hardcoded anon JWT removed from `trigger_auto_dispatch_new_job` — trigger now uses the shared cron secret and `booking-actions` accepts `x-cron-secret`.
- ✅ P2 · `integration-health-check` extended again with A2P 10DLC status probe (SignalWire campaign REJECTED/FAILED/EXPIRED/SUSPENDED). Coverage now 7/8; Upload-Post deferred (no API-key column yet).
- ℹ️ P3 · "`.lovable/plan.md` cron list is stale" downgraded to false positive — `plan.md` never contained a live cron enumeration; live DB now shows 26 active `aura-*` jobs (7 legacy dupes removed).
- ✅ P1 · `setup_progress` writer shipped — new `company_setup_step_overrides` table + `setSetupStepOverride()` writer in `useSetupProgress`. Manual overrides OR-merge over the derived signals so admins can force a step complete.
- ✅ P1 · Specialist tier gate drift eliminated — edge `SPECIALIST_MIN_TIER` in `ai-agent-chat` now derives from `INDUSTRY_SPECIALIST_OPERATIVES` at `'free'` for every specialist, matching the client `SPECIALIST_MIN_TIER='free'` and the specialists-all-plans standard.
- ℹ️ P1 · "No SECURITY DEFINER public company listing RPC" downgraded to false positive — `get_company_public_info(text)` and `_by_id(uuid)` exist as SECURITY DEFINER with anon EXECUTE grants and are already used by `PublicBooking.tsx`.
- ✅ P3 · `LEGACY_AGENT_MAP` extended with reception (receptionist/emergency/intake/faq), business (estimate/payments), and analytics aliases so every pre-consolidation ID normalizes to a consolidated operative.
- ℹ️ P0 · "Unified 14-day availability scan not implemented" downgraded to false positive — scan lives in `booking-actions/index.ts` `findNextAvailable()` (max_days=14) and is called by AI operatives via the `find_next_available` tool. Audit was looking at the wrong layer (UI).

## Top 10 to fix next

1. ~~P0 — Orchestrator proposals producer is a stub.~~ ✅ Fixed in earlier pass.
2. ~~P0 — 14-day availability scan missing.~~ ℹ️ False positive (exists in `booking-actions.findNextAvailable`).
3. **P1 — Duplicate cron jobs (7 pairs).** DB shows both legacy and `aura-*` copies running: `appointment-reminders-cron/-job/aura-appointment-reminders`, `aura-lead-follow-up-reminders`+`aura-lead-followup-reminders`, `check-unsubscribe-alerts-hourly`+`aura-check-unsubscribe-alerts`, `daily-cost-alerts`+`aura-cost-alerts`, `daily-trial-reminders`+`aura-trial-reminders`, `send-weekly-digests-daily`+`aura-weekly-digest`. Double-firing = duplicate emails/SMS.
4. **P1 — Duplicate inventory trigger.** `public.inventory_items` has both `trg_inventory_low_stock` and `trg_inventory_low_stock_alert`.
5. **P1 — `integration-health-check` only probes 2 of 8 providers.** ElevenLabs, Resend, Tavily, Stripe, A2P 10DLC, Upload-Post, CRM providers fetched but never verified.
6. **P1 — `dispatch-integrations` and `verify-insurance` edge functions never invoked.** No cron, trigger, or client caller — declared but dead.
7. ~~P1 — Specialist tier gate drift.~~ ✅ Fixed this pass.
8. **P1 — Fast-Start wizard has no tier-based redirect.** `OnboardingForm.tsx` never branches on subscription tier. Deferred: needs product decision on per-tier destinations.
9. ~~P1 — Public company listing RPC.~~ ℹ️ False positive (already implemented).
10. ~~P1 — `setup_progress` writer.~~ ✅ Fixed this pass.

## By area

### AI agents / operatives (5 findings)

- P1 · `analytics_intelligence` operative has no scheduled firing path; only event-driven.
- P2 · `EVENT_ROUTING` has zero entries for any of the 14 specialist operatives — either intentional (document) or a gap.
- P3 · `useConsoleAgentMetrics` has no specialist-operative bindings; `SpecialistOperativesConsole` cannot render usage stats.
- P3 · `LEGACY_AGENT_MAP` maps only 17 of the pre-consolidation 24 agent IDs.

### Automations (9 findings)

- P0 · `orchestrator-proposals` producer is a stub — see Top 10.
- P1 · `dispatch-integrations`, `verify-insurance` are orphan edge functions.
- P1 · Duplicate cron jobs (7 pairs).
- P1 · Duplicate inventory trigger.
- P2 · `send-email-guarded`, `campaign-track` appear unreferenced outside their own folder — confirm or remove.
- P2 · CRM "Sync Now" button in `CRMIntegration.tsx` not relabeled after `aura-crm-sync-leads` cron shipped.
- P2 · Manual "Run Now"/"Generate Now" in `Campaigns.tsx`, `CampaignDetail.tsx`, `MarketingSalesAgentConsole.tsx`, `GoogleCalendarSettings.tsx` still have no cron backstop for some paths.
- P3 · `.lovable/plan.md` live cron list is stale — only lists 9 of 33 live jobs.
- P3 · Hardcoded long-lived anon JWT embedded in `20260710162747` migration (secrets hygiene).

### Analytics / dashboards (2 findings)

- P1 · Memory claims 8-tab dashboard suite; `Analytics.tsx` has 6 tabs.
- P3 · No test coverage for `orchestrator-proposals` insert→read cycle.

### Integrations / settings (2 findings)

- P1 · `integration-health-check` only checks Google Cal + SignalWire (6 providers unchecked).
- P2 · No tier lock/unlock badge component wired into any `src/pages/integrations/*` page.

### Billing / pricing (4 findings)

- P2 · `create-checkout` onboarding price IDs not runtime-asserted against `launchPricing.ts` values (memory TODO now marked stale but no test).
- P2 · Memory references "Tee*"/"Tdvk*" Stripe price ID naming — actual IDs are `price_1TmJ2*`/`price_1TqgF*`; memory copy is stale.
- P3 · Trial length `60` and "60-Day Live Trial" strings duplicated across `create-checkout`, `trial-reminders`, `submit-onboarding`, `send-company-welcome` — no shared constant.
- P3 · `check-subscription` returns 200 + `subscribed: false` on internal errors, masking outages.

### Onboarding & setup (3 findings)

- P1 · Fast-Start wizard tier redirect missing.
- P1 · No `setup_progress` writer; all completion inferred.
- P3 · 10DLC not a discrete SignUp step — only linked from `Campaigns`, `SMSLogs`, `Subscription`.

### Customer-facing (6 findings)

- P0 · 14-day availability scan missing.
- P1 · Public company listing RPC missing.
- P2 · `useMultiAgentChat` invokes `ai-agent-chat`, not the `multi-agent-chat` function referenced in docs — one of the two is stale.
- P2 · `industryVoiceGreetings.ts` covers 27 keys and includes stray `templates` key; needs 1:1 mapping to all 28 packs.
- P3 · Chat widget: only 2 of 3 documented install methods discoverable.
- P3 · Technician FAB uses hardcoded `bottom-20` offset — no `env(safe-area-inset-bottom)`.

### Field ops (1 finding)

- P3 · `trg_auto_dispatch_new_job` correctly gated; migration embeds anon JWT (already flagged under Automations).

### Industry packs (3 findings)

- P2 · `industryKpiLabels.ts` OVERRIDES covers ~21 of 28 packs; `SIMPLE_KPI_BY_INDUSTRY` covers 11. Rest fall to cluster defaults.
- P2 · `quote_template`/`invoice_template`/`agent_prompt_deltas` live in `industry_blueprints` DB table; no seed migration populates them for the 28 packs — likely empty in prod.
- P3 · `industryEmptyStates.ts` structure suggests only 1 industry key branches per-industry.

### Cross-cutting hygiene (3 findings)

- P2 · 685 raw hex color literals outside marketing/dark surfaces (Cyber-Sentry standard drift).
- P3 · No memory doc actually exists at `mem://architecture/pwa-caching-and-scope-standard`; `public/sw.js` has zero fetch/cache/scope logic.
- P3 · `20260709232843` migration has 2 GRANT statements with no matching CREATE TABLE — confirm targets exist elsewhere.

### Notifications (1 finding)

- P3 · 4-channel staff notification (push/email/SMS/in-app bell) fan-out not per-channel-verified end-to-end.

### Naming (1 finding)

- P3 · `PricingComparisonTable.tsx:49` still contains "Central control center" wording — one residual naming-drift hit.

---

## Method notes

- 4 parallel background exploration passes (agents+automations, features+onboarding, customer+field ops, marketing+settings+billing+hygiene).
- DB verified: `cron.job` (33 rows), `pg_trigger` (public schema, all triggers enumerated).
- Nothing fixed in this pass. `src/lib/auditFindings.ts` updated with `status: "open"` entries so `/audit` reflects the new run.

## Recommended next slice

**Slice A — safe cleanup (S/M, no logic risk):** remove duplicate cron jobs (#3), drop duplicate inventory trigger (#4), remove/relabel CRM Sync Now button, update `.lovable/plan.md` cron list, remove hardcoded anon JWT from migration → replace with cron secret pattern.

**Slice B — big UX unlock (L):** implement real `orchestrator-proposals` detection logic (#1) so the approve/reject queue starts filling.

**Slice C — booking correctness (M):** ship the 14-day scan (#2) + public company listing RPC (#9).

**Slice D — polish (M):** finish `integration-health-check` provider coverage (#5), remove/wire orphan edge functions (#6), consolidate specialist tier gate to shared module (#7).